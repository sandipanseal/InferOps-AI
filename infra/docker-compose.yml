services:
  postgres:
    # pgvector/pgvector:pg16 = upstream postgres:16 + pgvector extension prebuilt.
    # Required because backend/app/core/rag_service.py uses pgvector for RAG
    # (replaces the standalone Qdrant service that used to live in this file).
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: inferops
      POSTGRES_USER: inferops
      POSTGRES_PASSWORD: inferops_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build:
      context: ../backend
    ports:
      - "8000:8000"
    env_file:
      - path: ../.env
        required: false
    environment:
      DATABASE_URL: postgresql+asyncpg://inferops:inferops_password@postgres:5432/inferops

      REDIS_URL: redis://redis:6379/0

      # QDRANT_URL kept for backward compatibility but no longer used —
      # RAG is now pgvector-backed via DATABASE_URL above.
      RAG_COLLECTION_NAME: inferops_knowledge
      EMBEDDING_MODEL_NAME: sentence-transformers/all-MiniLM-L6-v2

      OLLAMA_BASE_URL: http://host.docker.internal:11434
      OLLAMA_MODEL: llama3.1:8b

      OLLAMA_CLOUD_BASE_URL: https://ollama.com
      OLLAMA_CLOUD_API_KEY: ${OLLAMA_CLOUD_API_KEY:-}
      OLLAMA_CLOUD_MODEL: gpt-oss:120b-cloud

      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    volumes:
      - ../backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    depends_on:
      - postgres
      - redis

  frontend:
    build:
      context: ../frontend
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://localhost:8000
    volumes:
      - ../frontend:/app
      - /app/node_modules
      - /app/.next
    command: npm run dev
    depends_on:
      - backend

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    depends_on:
      - backend

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3001:3000"
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
      - ./grafana/dashboards:/var/lib/grafana/dashboards
    depends_on:
      - prometheus

volumes:
  postgres_data:
  grafana_data: